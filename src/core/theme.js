import { DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: '#00C5DD',
    primary: '#00C5DD',
    secondary: '#FFFFFF',
    error: '#f13a59',
  },
}
